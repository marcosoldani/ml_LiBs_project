% ── Encoding & lingua ──────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[italian,english]{babel}

% ── Layout ─────────────────────────────────────────────────────────────────
\usepackage[a4paper,top=2.7cm,bottom=2.7cm,left=2.6cm,right=2.6cm]{geometry}
\usepackage{setspace}
\onehalfspacing
\usepackage{microtype}
\usepackage{parskip}

% ── Matematica & simboli ───────────────────────────────────────────────────
\usepackage{amsmath, amssymb, mathtools}
\usepackage{siunitx}
\sisetup{
  detect-all,
  per-mode=symbol,
  output-decimal-marker={.}
}

% ── Tabelle, liste, codice ─────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{enumitem}
\setlist{topsep=2pt, itemsep=2pt, parsep=2pt}

\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1F4E79}
\definecolor{codebg}{HTML}{F4F6F8}
\definecolor{codestring}{HTML}{1A7F37}
\definecolor{codekw}{HTML}{8250DF}
\definecolor{codecomment}{HTML}{6E7781}

\usepackage{listings}

% Custom lightweight YAML highlighter (listings has no built-in YAML).
\lstdefinelanguage{YAML}{
  keywords={true, false, null},
  keywordstyle=\color{codekw}\bfseries,
  sensitive=true,
  comment=[l]{\#},
  morestring=[b]",
  morestring=[b]',
  literate=
    {:}{{{\color{codekw}{:}}}}{1}
    {-}{{{\color{codekw}{-}}}}{1},
}

% Custom lightweight JSON highlighter.
\lstdefinelanguage{JSON}{
  keywords={true, false, null},
  keywordstyle=\color{codekw}\bfseries,
  sensitive=false,
  morestring=[b]",
  literate=
    {\{}{{{\color{codekw}\{}}}{1}
    {\}}{{{\color{codekw}\}}}}{1}
    {[}{{{\color{codekw}[}}}{1}
    {]}{{{\color{codekw}]}}}{1}
    {:}{{{\color{codekw}{:}}}}{1}
    {,}{{{\color{codekw}{,}}}}{1},
}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codebg},
  framerule=0pt,
  xleftmargin=8pt,
  xrightmargin=8pt,
  aboveskip=4pt,
  belowskip=4pt,
  showstringspaces=false,
  breaklines=true,
  keywordstyle=\color{codekw}\bfseries,
  commentstyle=\color{codecomment}\itshape,
  stringstyle=\color{codestring},
  language=Python,
  tabsize=4,
}

% ── Figure ─────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{subcaption}
\usepackage{float}
\usepackage{caption}
\captionsetup{font=small, labelfont=bf}

% ── Box pedagogici (Key Insight) ───────────────────────────────────────────
\usepackage[most]{tcolorbox}
\newtcolorbox{insight}[1][]{
  enhanced,
  colback=accent!5!white,
  colframe=accent!75!black,
  fonttitle=\bfseries,
  title={Key Insight},
  rounded corners,
  drop fuzzy shadow=accent!20!gray,
  boxrule=0.5pt,
  #1
}

% ── Riferimenti & link ─────────────────────────────────────────────────────
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Battery GEIS MLOps Platform},
  pdfauthor={Davide Corso, Marco Soldani},
  pdfsubject={Progetto di Semestre — MLOps},
  pdfkeywords={MLOps, EIS, GEIS, Lithium-Ion, Battery, Drift Detection, MLflow}
}
\usepackage[noabbrev,capitalise,italian]{cleveref}

% ── Bibliografia ───────────────────────────────────────────────────────────
\usepackage{csquotes}
\usepackage[backend=biber,style=numeric,sorting=none,maxbibnames=99,minbibnames=99]{biblatex}

% ── Glossari ───────────────────────────────────────────────────────────────
\usepackage[acronym,toc,nonumberlist,nopostdot,nogroupskip]{glossaries}
\setglossarystyle{long}
\makeglossaries

% ── Header / footer ────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\setlength{\headheight}{14pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\nouppercase\leftmark}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── Numeri di capitolo più compatti ────────────────────────────────────────
\usepackage{titlesec}
\titleformat{\chapter}[hang]
  {\Huge\bfseries\color{accent}}
  {\thechapter\hspace{12pt}}
  {0pt}
  {}
\titlespacing*{\chapter}{0pt}{-30pt}{20pt}

% ── Comandi custom ─────────────────────────────────────────────────────────
\newcommand{\milliohm}{\si{\milli\ohm}}
\newcommand{\codefile}[1]{\texttt{\small#1}}
